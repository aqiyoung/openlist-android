<resources>
    <!-- 主题只保留窗口背景色; 状态栏/导航栏由 Activity 里 WindowInsetsControllerCompat 控制 -->
    <style name="Theme.OpenList" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
